# homebrew-whoopterm

Homebrew tap for [whoopterm](https://github.com/idossha/whoopterm) — a WHOOP fitness dashboard for the terminal.

## Installation

```bash
brew tap idossha/whoopterm
brew install whoopterm
```

Or as a one-liner:

```bash
brew install idossha/whoopterm/whoopterm
```
